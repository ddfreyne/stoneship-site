base_url: "http://stoneship.org"

enable_output_diff: true

prune:
  auto_prune: true

data_sources:
  - type: filesystem_unified

deploy:
  public:
    kind: rsync
    dst:  "bhakti:/srv/http/stoneship.org"
