base_url: "http://stoneship.org"

enable_output_diff: true

prune:
  auto_prune: true
  exclude: [ '.git' ]

data_sources:
  - type: filesystem_unified

deploy:
  public:
    kind: git
    remote: git@bhakti:/srv/git/stoneship.org.git
    branch: prod
    forced: true
